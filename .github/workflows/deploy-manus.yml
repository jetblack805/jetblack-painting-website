name: Deploy to Manus

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Trigger Manus Deployment
        run: |
          echo "✅ Deployment triggered for Manus"
          echo "Commit: ${{ github.sha }}"
          echo "Sitemap and static files will be deployed"
